version: '3.8'

services:
  ansible:
    build: .
    container_name: ansible_container
    volumes:
      - "/Users/wmasiew2/DockerTesting/playbooks:/data"  # Playbooks
      - "/Users/wmasiew2/DockerTesting/.ssh:/root/.ssh:ro"  # SSH keys
    working_dir: /data
    tty: true
